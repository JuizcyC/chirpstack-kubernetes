apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: us-chirpstack-postgres-data
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-csi
  volumeName: us-chirpstack-postgres-data
  resources:
    requests:
      storage: 4Gi