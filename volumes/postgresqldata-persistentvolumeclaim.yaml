apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresqldata
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: default
  volumeName: us-chirpstack-postgres-pv
  resources:
    requests:
      storage: 1Gi
